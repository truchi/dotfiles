# Install submodule
git submodule update --init --recursive

# Rebuild font cache
fc-cache -f ~/.fonts

# TODO install spacemacs
# TODO what if already installed?
# Maybe prompt 'First install?' then install all the things
# (npm, node, spacemacs, tern, wakatime, ...)
# and enable services (emacs, ...?)
