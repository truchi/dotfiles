- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    # ~/.zshrc: zsh/zshrc.zsh
    ~/.spacemacs: spacemacs/spacemacs.el
    ~/.fonts: fonts/

- shell:
  - [./postinstall.zsh, Post install]
